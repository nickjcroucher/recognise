name: recognise_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # python
  - python>=3.8
  - multiprocess
  # alignment
  - biopython>=1.69
  - lastz
  - multiz
  - minigraph
  - gfatools
  # recombination
  - gubbins>=3.2.1
  - 3seq >=1.8
  # testing
  - pytest
  - pytest-cov
